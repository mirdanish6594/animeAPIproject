<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= anime.title %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #1a1a2e;
            color: #e0e0e0;
            margin: 0;
            padding: 2rem;
        }
        .container {
            max-width: 1100px;
            margin: auto;
            background: #16213e;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        }
        .back-link {
            display: inline-block;
            margin-bottom: 2rem;
            color: #00aaff;
            text-decoration: none;
            font-weight: 600;
        }
        .details-grid {
            display: grid;
            grid-template-columns: 300px 1fr;
            gap: 2rem;
        }
        .details-grid img {
            width: 100%;
            border-radius: 8px;
        }
        .anime-info h1 {
            margin-top: 0;
            font-size: 2.5rem;
            color: #ffffff;
        }
        .stats { display: flex; gap: 1.5rem; margin: 1rem 0; }
        .stat { background: #0f3460; padding: 0.5rem 1rem; border-radius: 20px; font-size: 0.9rem; }
        .synopsis { line-height: 1.7; }
        .trailer { margin-top: 2rem; }
        .trailer iframe { width: 100%; height: 400px; border: none; border-radius: 8px; }
        @media (max-width: 768px) {
            .details-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <div class="container">
        <a href="/" class="back-link">&larr; Back to Search</a>
        <div class="details-grid">
            <div class="anime-poster">
                <img src="<%= anime.images.jpg.large_image_url %>" alt="<%= anime.title %> Poster">
            </div>
            <div class="anime-info">
                <h1><%= anime.title_english || anime.title %></h1>
                <div class="stats">
                    <span class="stat">⭐ <%= anime.score || 'N/A' %></span>
                    <span class="stat">Rank: #<%= anime.rank || 'N/A' %></span>
                    <span class="stat"><%= anime.type %> - <%= anime.episodes || '?' %> eps</span>
                </div>
                <h3>Synopsis</h3>
                <p class="synopsis"><%= anime.synopsis %></p>
                
                <% if (anime.trailer && anime.trailer.embed_url) { %>
                    <div class="trailer">
                        <h3>Trailer</h3>
                        <iframe src="<%= anime.trailer.embed_url %>" allowfullscreen></iframe>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
