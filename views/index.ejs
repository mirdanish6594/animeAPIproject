<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Anime Search</title>
    <style>
        /* Basic styling for demonstration */
        body { font-family: sans-serif; background-color: #f4f4f9; color: #333; padding: 20px; }
        h1 { text-align: center; }
        form { display: flex; justify-content: center; margin-bottom: 30px; }
        input[type="text"] { width: 300px; padding: 10px; border-radius: 5px 0 0 5px; border: 1px solid #ccc; }
        button { padding: 10px; border: 1px solid #007bff; background-color: #007bff; color: white; border-radius: 0 5px 5px 0; cursor: pointer; }
        .results-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 20px; }
        .anime-card { background: white; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); text-align: center; padding: 10px; }
        .anime-card img { max-width: 100%; border-radius: 5px; }
        .message { text-align: center; font-size: 1.2em; color: #555; }
    </style>
</head>
<body>
    <h1>Search for Anime</h1>
    <form action="/" method="GET">
        <input type="text" name="q" placeholder="e.g., Attack on Titan" value="<%= query %>" required>
        <button type="submit">Search</button>
    </form>

    <% if (locals.error) { %>
        <p class="message" style="color: red;"><%= error %></p>
    <% } %>

    <div class="results-container">
        <% if (animeData.length > 0) { %>
            <div class="results-grid">
                <% animeData.forEach(anime => { %>
                    <div class="anime-card">
                        <img src="<%= anime.images.jpg.image_url %>" alt="<%= anime.title %>">
                        <h3><%= anime.title %></h3>
                        <p>Score: <%= anime.score || 'N/A' %></p>
                    </div>
                <% }) %>
            </div>
        <% } else if (query) { %>
            <p class="message">No results found for "<%= query %>". Please try another search.</p>
        <% } else { %>
            <p class="message">Enter an anime name above to get started!</p>
        <% } %>
    </div>
</body>
</html>
